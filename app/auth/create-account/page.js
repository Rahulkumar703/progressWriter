import SignupForm from "@/components/signupForm";

export const metadata = {
  title: "Create Account",
  description: "Create a new account or use social accounts.",
};

const LoginPage = () => {
  return <SignupForm />;
};

export default LoginPage;
