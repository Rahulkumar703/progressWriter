import LoginFom from "@/components/loginForm";

export const metadata = {
  title: "Login",
  description: "Login with email and password or use social accounts.",
};
const LoginPage = () => {
  return <LoginFom />;
};

export default LoginPage;
